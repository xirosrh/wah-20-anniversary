mapscripts CosararaRoom_MapScripts {
     MAP_SCRIPT_ON_WARP_INTO_MAP_TABLE [
        VAR_TEMP_8, 0: CosararaRoom_OnWarp_PlayerTurnNorth
    ]
    MAP_SCRIPT_ON_LOAD: CosararaRoom_OnLoad
    MAP_SCRIPT_ON_TRANSITION : CosararaRoom_OnTransition
}


script CosararaRoom_OnLoad {
    if(flag(FLAG_DEFEATED_ADMIN_COSARARA)){
        call(WahRoomsShared_SetDoorAsOpen)
    }
    end
}

script CosararaRoom_OnWarp_PlayerTurnNorth {
    turnobject(LOCALID_PLAYER, DIR_NORTH)
    end
}

script CosararaRoom_OnTransition {
    call(WahRoomsShared_Kecleon_SetSala_Cosarara)
    end
}

script CosararaRoom_EventScript_Cosarara {
    lock
    if(flag(FLAG_DEFEATED_ADMIN_COSARARA)) {
        playse(SE_PIN)
        applymovementandwait(LOCALID_COSARARAROOM_COSARARA, moves(face_player, emote_exclamation_mark, delay_16, face_up, delay_16 * 2))
        call(CosararaRoom_EventScript_LockPC)
        delay(20)
        faceplayer
        msgbox(format("And you are? Ahhh yes, now I remember you, {PLAYER}.\pI wish you well!"), MSGBOX_AUTOCLOSE, speaker = SP_NAME_COSARARA)
        applymovementandwait(LOCALID_COSARARAROOM_COSARARA, Common_Movement_FaceUp)
        delay(20)
        call(CosararaRoom_EventScript_UnlockPC)
        release
        end
    }
    msgbox(format("I did it."), MSGBOX_AUTOCLOSE, speaker = SP_NAME_COSARARA)
    playse(SE_PIN)
    applymovementandwait(LOCALID_COSARARAROOM_COSARARA, Common_Movement_HappyWithDelay48)
    delay(20)
    msgbox(format("I've just finished an unauthorized backup of all the community's records.\pWe built it all together, so I think it's worth preserving."), MSGBOX_AUTOCLOSE, speaker = SP_NAME_COSARARA)
    delay(20)
    faceplayer
    playse(SE_PIN)
    applymovementandwait(LOCALID_COSARARAROOM_COSARARA, moves(face_player, emote_question_mark, delay_16, face_up, delay_16 * 2))
    msgbox(format("I'm going to lock my computer…"), MSGBOX_AUTOCLOSE, speaker = SP_NAME_COSARARA)
    call(CosararaRoom_EventScript_LockPC)
    delay(20)
    faceplayer
    msgbox(format("Wait, who are you again? Did you change your nick recently?\pI lose track so easily… my memory just can't keep up!"), MSGBOX_AUTOCLOSE, speaker = SP_NAME_COSARARA)
    playse(SE_PIN)
    applymovementandwait(LOCALID_COSARARAROOM_COSARARA, Common_Movement_SweatDropWithDelay48)
    delay(20)
    specialvar(VAR_RESULT, GetPlayerFacingDirection)
    switch(var(VAR_RESULT)) {
        case DIR_NORTH:
            applymovementandwait(LOCALID_PLAYER, Common_Movement_WalkInPlaceUp)
            break
        case DIR_WEST:
            applymovementandwait(LOCALID_PLAYER,Common_Movement_WalkInPlaceLeft)
            break
        case DIR_EAST:
            applymovementandwait(LOCALID_PLAYER, Common_Movement_WalkInPlaceRight)
            break
    }
    msgbox(format("So you're {PLAYER}, and you want to battle? Challenge accepted!"), MSGBOX_AUTOCLOSE, speaker = SP_NAME_COSARARA)
    playse(SE_PIN)
    applymovementandwait(LOCALID_COSARARAROOM_COSARARA, moves(emote_versus))
    waitse
    call(CosararaRoom_EventScript_CosararaBattle)
    setflag(FLAG_DEFEATED_ADMIN_COSARARA)
    delay(20)
    msgbox(format("That was a good battle, {PLAYER}. I think I'll actually remember you this time.\pJust don't go changing your nick - it's so hard for me to tell who's who!\pYou may proceed now."), MSGBOX_AUTOCLOSE, speaker = SP_NAME_COSARARA)
    call(WahRoomsShared_OpenDoor)
    delay(20)
    applymovementandwait(LOCALID_COSARARAROOM_COSARARA, Common_Movement_FaceUp)
    delay(20)
    call(CosararaRoom_EventScript_UnlockPC)
    call(WahRoomsShared_EventScript_AfterBattle)
    release
    end
}

script CosararaRoom_EventScript_CosararaBattle {
    if (!flag(FLAG_WAH_CHALLENGE_COMPLETED)) {
        trainerbattle_no_intro(TRAINER_WAH_ADMIN_COSARARA_MAIN, CosararaRoom_Text_CosararaDefeated)
    } else {
        setvar(VAR_0x8004, TEAM_INDEX_COSARARA)
        special(ShouldUseAlternativeTeam)
        if (var(VAR_RESULT) == 0) {
            trainerbattle_no_intro(TRAINER_WAH_ADMIN_COSARARA_MAIN, CosararaRoom_Text_CosararaDefeated)
        } else {
            trainerbattle_no_intro(TRAINER_WAH_ADMIN_COSARARA_ALTERNATIVE, CosararaRoom_Text_CosararaDefeated)
        }
    }
    return
}

text CosararaRoom_Text_CosararaDefeated {
    format("This battle made me very happy.")
}

script CosararaRoom_EventScript_LockPC {
    setmetatile(1, 1, METATILE_room_cosarara_PC_LOCKED_1, TRUE)
    setmetatile(2, 1, METATILE_room_cosarara_PC_LOCKED_2, TRUE)
    setmetatile(3, 1, METATILE_room_cosarara_PC_LOCKED_3, TRUE)
    setmetatile(4, 1, METATILE_room_cosarara_PC_LOCKED_4, TRUE)
    setmetatile(5, 1, METATILE_room_cosarara_PC_LOCKED_5, TRUE)
    setmetatile(1, 2, METATILE_room_cosarara_PC_LOCKED_6, TRUE)
    setmetatile(2, 2, METATILE_room_cosarara_PC_LOCKED_7, TRUE)
    setmetatile(3, 2, METATILE_room_cosarara_PC_LOCKED_8, TRUE)
    setmetatile(4, 2, METATILE_room_cosarara_PC_LOCKED_9, TRUE)
    setmetatile(5, 2, METATILE_room_cosarara_PC_LOCKED_10, TRUE)
    special(DrawWholeMapView)
    return
}

script CosararaRoom_EventScript_UnlockPC {
    setmetatile(1, 1, METATILE_room_cosarara_PC_MATRIX_1, TRUE)
    setmetatile(2, 1, METATILE_room_cosarara_PC_MATRIX_2, TRUE)
    setmetatile(3, 1, METATILE_room_cosarara_PC_MATRIX_3, TRUE)
    setmetatile(4, 1, METATILE_room_cosarara_PC_MATRIX_4, TRUE)
    setmetatile(5, 1, METATILE_room_cosarara_PC_MATRIX_5, TRUE)
    setmetatile(1, 2, METATILE_room_cosarara_PC_MATRIX_6, TRUE)
    setmetatile(2, 2, METATILE_room_cosarara_PC_MATRIX_7, TRUE)
    setmetatile(3, 2, METATILE_room_cosarara_PC_MATRIX_8, TRUE)
    setmetatile(4, 2, METATILE_room_cosarara_PC_MATRIX_9, TRUE)
    setmetatile(5, 2, METATILE_room_cosarara_PC_MATRIX_10, TRUE)
    special(DrawWholeMapView)
    return
}

script CosararaRoom_EventScript_Server1 {
    msgbox(format("It looks like an old server machine.\pIt has a control panel with buttons and lights."), MSGBOX_SIGN)
    end
}

script CosararaRoom_EventScript_Server2 {
    msgbox(format("A small label reads: “DO NOT TURN OFF.”"), MSGBOX_SIGN)
    end
}

script CosararaRoom_EventScript_Server3 {
    msgbox(format("The constant movement of the lights is hypnotic."), MSGBOX_SIGN)
    end
}

script CosararaRoom_EventScript_Box1 {
    msgbox(format("The box is full of perfectly coiled cables."), MSGBOX_SIGN)
    end
}

script CosararaRoom_EventScript_Box2 {
    msgbox(format("There are disks and electronic components stored with care."), MSGBOX_SIGN)
    end
}

script CosararaRoom_EventScript_Box3 {
    msgbox(format("A small label reads: “Obsolete hardware.”"), MSGBOX_SIGN)
    end
}

script CosararaRoom_EventScript_Box4 {
    msgbox(format("The box is empty."), MSGBOX_SIGN)
    end
}

script CosararaRoom_EventScript_Boxes {
    msgbox(format("There are several boxes stacked on top of each other.\pIt looks like a makeshift storeroom."), MSGBOX_SIGN)
    end
}

script CosararaRoom_EventScript_OldComputers {
    msgbox(format("They're computers too old to still be used.\pIt seems no one wanted to get rid of them."), MSGBOX_SIGN)
    end
}

script CosararaRoom_EventScript_SuperComputer {
    msgbox(format("Endless columns of symbols scroll across the screens.\pIt all looks like a huge data matrix."), MSGBOX_SIGN)
    end
}

script CosararaRoom_EventScript_Notes1 {
    msgbox(format("There are several papers lying on the floor.\pThey seem to have the username and password for the Whack a Hack server."), MSGBOX_SIGN)
}

script CosararaRoom_EventScript_Notes2 {
    msgbox(format("There are several papers lying on the floor.\pThey have an inventory of the server room."), MSGBOX_SIGN)
}

script CosararaRoom_EventScript_Notes3 {
    msgbox(format("There are several papers lying on the floor.\pThey seem to have several printed HTML pages from the Whack a Hack site.\pIt's dated 2014."), MSGBOX_SIGN)
}